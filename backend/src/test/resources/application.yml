# 테스트 전용 설정 — H2 인메모리 (PostgreSQL 호환 모드)
spring:
  datasource:
    url: jdbc:h2:mem:erdtest;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=KEY,COMMENT
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
